# E2E integration test stack.
#
# Starts postgres + redis + elasticsearch + api + web with seeded test data
# for running Playwright E2E tests against real services.
#
# Usage:
#   docker compose -f docker-compose.e2e.yml up --build -d
#   cd apps/web && npx playwright test
#   docker compose -f docker-compose.e2e.yml down -v

version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    environment:
      - POSTGRES_DB=leyes_mx_test
      - POSTGRES_USER=leyes_mx
      - POSTGRES_PASSWORD=${POSTGRES_PASSWORD:?set POSTGRES_PASSWORD for local compose}
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          memory: 256M
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U leyes_mx"]
      interval: 3s
      timeout: 3s
      retries: 5

  redis:
    image: redis:7-alpine
    deploy:
      resources:
        limits:
          cpus: '0.25'
          memory: 128M
        reservations:
          memory: 64M
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 3s
      timeout: 3s
      retries: 5

  elasticsearch:
    image: docker.elastic.co/elasticsearch/elasticsearch:8.17.0
    environment:
      - node.name=e2e-es
      - cluster.name=e2e-cluster
      - discovery.type=single-node
      - bootstrap.memory_lock=true
      # E2E stack: throwaway test data, no host port exposed, lifetime is one
      # CI run. Auth disabled to keep startup under the 30s healthcheck window
      # and avoid leaking creds through CI env. Production / dev compose
      # enables xpack.security.
      - xpack.security.enabled=false
      - xpack.security.enrollment.enabled=false
      - "ES_JAVA_OPTS=-Xms384m -Xmx384m"
    ulimits:
      memlock:
        soft: -1
        hard: -1
    deploy:
      resources:
        limits:
          cpus: '1.0'
          memory: 768M
        reservations:
          memory: 512M
    healthcheck:
      test: ["CMD-SHELL", "curl -fs http://localhost:9200/_cluster/health || exit 1"]
      interval: 10s
      timeout: 10s
      retries: 18
      start_period: 30s

  api:
    build:
      context: .
      dockerfile: apps/indigo/Dockerfile
    command: >
      sh -c "python manage.py migrate --noinput &&
             python manage.py index_laws --tier federal --limit 10 &&
             python manage.py runserver 0.0.0.0:8000"
    environment:
      - DEBUG=True
      - DJANGO_SECRET_KEY=e2e-test-key-not-for-production
      - ALLOWED_HOSTS=*
      - CORS_ALLOWED_ORIGINS=http://localhost:3000
      - DB_ENGINE=django.db.backends.postgresql
      - DB_NAME=leyes_mx_test
      - DB_USER=leyes_mx
      - DB_PASSWORD=test_pw
      - DB_HOST=postgres
      - DB_PORT=5432
      - CELERY_BROKER_URL=redis://redis:6379/0
      - ES_HOST=http://elasticsearch:9200
      - STORAGE_BACKEND=local
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          memory: 256M
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
      elasticsearch:
        condition: service_healthy
    ports:
      - "8000:8000"

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    environment:
      - NEXT_PUBLIC_API_URL=http://localhost:8000/api/v1
    deploy:
      resources:
        limits:
          cpus: '0.5'
          memory: 512M
        reservations:
          memory: 256M
    depends_on:
      - api
    ports:
      - "3000:3000"
