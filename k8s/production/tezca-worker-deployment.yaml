apiVersion: apps/v1
kind: Deployment
metadata:
  name: tezca-worker
  labels:
    app.kubernetes.io/name: tezca-worker
    app.kubernetes.io/part-of: tezca
spec:
  replicas: 1
  revisionHistoryLimit: 3
  selector:
    matchLabels:
      app.kubernetes.io/name: tezca-worker
  template:
    metadata:
      labels:
        app.kubernetes.io/name: tezca-worker
        app.kubernetes.io/part-of: tezca
    spec:
      imagePullSecrets:
        - name: ghcr-credentials
      securityContext:
        runAsNonRoot: true
        seccompProfile:
          type: RuntimeDefault
      containers:
        - name: worker
          image: ghcr.io/madfam-org/tezca/api
          securityContext:
            privileged: false
            allowPrivilegeEscalation: false
            readOnlyRootFilesystem: false
            runAsUser: 1001
            capabilities:
              drop: ["ALL"]
          command:
            - celery
            - -A
            - apps.indigo
            - worker
            - --loglevel=info
            - --concurrency=4
          env:
            - name: DJANGO_SECRET_KEY
              valueFrom:
                secretKeyRef:
                  name: tezca-secrets
                  key: DJANGO_SECRET_KEY
            - name: DB_USER
              valueFrom:
                secretKeyRef:
                  name: tezca-secrets
                  key: DB_USER
            - name: DB_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: tezca-secrets
                  key: DB_PASSWORD
            - name: DB_HOST
              valueFrom:
                secretKeyRef:
                  name: tezca-secrets
                  key: DB_HOST
            - name: DEBUG
              value: "False"
            - name: DB_ENGINE
              value: "django.db.backends.postgresql"
            - name: DB_NAME
              value: "tezca"
            - name: DB_PORT
              value: "5432"
            - name: CELERY_BROKER_URL
              value: "redis://tezca-redis:6379/0"
            - name: CELERY_RESULT_BACKEND
              value: "redis://tezca-redis:6379/0"
            - name: ES_HOST
              value: "http://tezca-es:9200"
            - name: SENTRY_DSN
              valueFrom:
                secretKeyRef:
                  name: tezca-secrets
                  key: SENTRY_DSN
                  optional: true
            - name: SENTRY_ENVIRONMENT
              value: "production"
            - name: STORAGE_BACKEND
              valueFrom:
                secretKeyRef:
                  name: tezca-r2-credentials
                  key: STORAGE_BACKEND
            - name: R2_ACCESS_KEY_ID
              valueFrom:
                secretKeyRef:
                  name: tezca-r2-credentials
                  key: R2_ACCESS_KEY_ID
            - name: R2_SECRET_ACCESS_KEY
              valueFrom:
                secretKeyRef:
                  name: tezca-r2-credentials
                  key: R2_SECRET_ACCESS_KEY
            - name: R2_ENDPOINT_URL
              valueFrom:
                secretKeyRef:
                  name: tezca-r2-credentials
                  key: R2_ENDPOINT_URL
            - name: R2_BUCKET_NAME
              valueFrom:
                secretKeyRef:
                  name: tezca-r2-credentials
                  key: R2_BUCKET_NAME
          livenessProbe:
            exec:
              command:
                - python
                - -c
                - "from apps.indigo.celery import app; app.control.ping(timeout=5)"
            initialDelaySeconds: 60
            periodSeconds: 60
            timeoutSeconds: 15
            failureThreshold: 3
          readinessProbe:
            exec:
              command:
                - python
                - -c
                - "from apps.indigo.celery import app; app.control.ping(timeout=5)"
            initialDelaySeconds: 30
            periodSeconds: 30
            timeoutSeconds: 15
            failureThreshold: 3
          resources:
            requests:
              cpu: 250m
              memory: 512Mi
            limits:
              cpu: 1000m
              memory: 2Gi
